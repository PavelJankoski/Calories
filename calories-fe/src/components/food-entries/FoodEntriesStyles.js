import {makeStyles} from "@material-ui/core/styles";

export const useFoodEntriesStyles = makeStyles(() => ({
    dailyThresholdsDiv: {
        marginTop: "2em"
    },
    dailyThresholdTitle: {
        marginBottom: "1em"
    }
}));