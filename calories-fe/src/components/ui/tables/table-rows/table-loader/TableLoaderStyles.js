import {makeStyles} from "@material-ui/core/styles";

export const useTableLoaderStyles = makeStyles(() => ({
    tableLoaderCell: {
        paddingTop: "1em",
        paddingBottom: "1em",
        textAlign: "center"
    }
}));