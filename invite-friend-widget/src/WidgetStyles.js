import {makeStyles} from "@material-ui/core";

export const useWidgetStyles = makeStyles(() => ({
    inviteButton: {
        width: "190px"
    },
    inputDiv: {
        padding: "1em",
        textAlign: "center"
    },
    sendInviteButton: {
        marginTop: "0.5em"
    }
}));
